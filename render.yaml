services:
  # ==========================================
  # Backend API (FastAPI + Uvicorn)
  # ==========================================
  - type: web
    name: privatechat-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: privatechat-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: COOKIE_SECURE
        value: "true"
      - key: COOKIE_SAMESITE
        value: "none"
      - key: CORS_ORIGINS
        fromService:
          type: web
          name: privatechat-frontend
          property: host

  # ==========================================
  # Frontend UI (Next.js)
  # ==========================================
  - type: web
    name: privatechat-frontend
    runtime: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm run start
    plan: free
    autoDeploy: true
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: privatechat-backend
          property: host

databases:
  # ==========================================
  # Managed PostgreSQL Database
  # ==========================================
  - name: privatechat-db
    databaseName: privatechat
    user: privatechat
    plan: free
